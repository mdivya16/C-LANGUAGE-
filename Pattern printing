```
#include <stdio.h>

void printPattern(int numbers[], int n) {
    for (int i = 0; i < n; i++) {
        printf("Block %d: %d\n", i + 1, numbers[i]);
        for (int j = 0; j < numbers[i]; j++) {
            printf("* ");
            if ((j + 1) % 10 == 0) {
                printf("\n");
            }
        }
        printf("\n\n");
    }
}

int main() {
    int numbers[] = {20, 15, 10, 5};
    int n = sizeof(numbers) / sizeof(numbers[0]);
    printPattern(numbers, n);
    return 0;
}
```
