#include <stdio.h>

int main()
{
    int a, b, r, q;
    printf("enter a, b values");
    scanf("%d%d",&a, &b); 
    while(b!=0)
    {
        r=a%b;
        q=a/b;
        a=b;
        b=r;
    }
    printf("gcd of a, b is%d",a); 
        
    

    return 0;
}
